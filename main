class Product:
    def __init__ (name, price):
        pattern = '^[a-zA-Z]+[0-9]+$'
        if re.fullmatch (pattern, name) is None:
            raise ValueError('nie wyszło')

        self.name= name
        self.price= price
